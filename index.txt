<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Abdul Fahath S — Marriage Biodata</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg-1: #f7fbff;
      --bg-2: #eef9f6;
      --card-bg: rgba(255,255,255,0.9);
      --muted: #6b7280;
      --accent: #2563eb;
      --glass: rgba(255,255,255,0.65);
      --radius: 14px;
      --shadow: 0 6px 18px rgba(16,24,40,0.08);
      --shadow-hover: 0 12px 30px rgba(16,24,40,0.12);
    }

    /* Page background */
    html,body{
      height:100%;
      margin:0;
      font-family: 'Poppins', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(160deg, var(--bg-1) 0%, #f1f6ff 20%, #fbfbff 40%, #ffffff 100%);
      color:#0f172a;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    /* Center layout */
    .wrap{
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:36px 20px;
    }

    .container{
      width:100%;
      max-width:860px;
      margin:0 auto;
      display:flex;
      flex-direction:column;
      align-items:center;
      gap:18px;
    }

    /* Header Card */
    .profile-card{
      width:100%;
      background: linear-gradient(180deg, rgba(255,255,255,0.9), rgba(255,255,255,0.82));
      border-radius: calc(var(--radius) + 6px);
      box-shadow: var(--shadow);
      padding:22px;
      text-align:center;
      transition:transform .28s ease, box-shadow .28s ease;
    }
    .profile-card:hover{
      transform:translateY(-6px);
      box-shadow: var(--shadow-hover);
    }

    /* Avatar */
    .avatar-wrap{
      position:relative;
      display:flex;
      justify-content:center;
      margin-top:-64px; /* pull avatar up */
    }

    .avatar {
      width:128px;
      height:128px;
      border-radius:999px;
      display:flex;
      align-items:center;
      justify-content:center;
      font-weight:700;
      color:white;
      background: linear-gradient(135deg,#4f46e5,#06b6d4);
      box-shadow: 0 8px 22px rgba(2,6,23,0.08);
      border: 6px solid rgba(255,255,255,0.85);
      overflow:hidden;
      position:relative;
    }

    .avatar img{
      width:100%;
      height:100%;
      object-fit:cover;
      display:block;
    }

    /* Upload button overlay */
    .upload-btn {
      position:absolute;
      right:-6px;
      bottom:-6px;
      background:linear-gradient(180deg,#fff,#f2f6ff);
      border-radius:10px;
      padding:6px;
      box-shadow: 0 6px 14px rgba(16,24,40,0.08);
      border:1px solid rgba(15,23,42,0.06);
      cursor:pointer;
      display:flex;
      align-items:center;
      gap:8px;
      font-size:13px;
      color:var(--accent);
      transition:transform .18s ease;
    }
    .upload-btn:hover{ transform:translateY(-4px); }

    .upload-btn input[type="file"]{
      display:none;
    }

    .name{
      font-size:20px;
      font-weight:700;
      margin-top:12px;
    }
    .profession{
      color:var(--muted);
      font-weight:600;
      margin-top:4px;
      font-size:14px;
    }

    /* Cards layout */
    .cards{
      width:100%;
      display:grid;
      grid-template-columns: 1fr;
      gap:14px;
      margin-top:6px;
    }

    .card{
      background: var(--card-bg);
      border-radius: var(--radius);
      padding:18px;
      box-shadow: var(--shadow);
      transition: transform .26s ease, box-shadow .26s ease;
      border: 1px solid rgba(15,23,42,0.03);
    }
    .card:hover{
      transform: translateY(-6px);
      box-shadow: var(--shadow-hover);
    }

    .card h3{
      margin:0 0 8px 0;
      font-size:16px;
      color:#0f172a;
    }
    .card p.lead{
      margin:0;
      color:var(--muted);
      font-weight:600;
      font-size:14px;
    }

    .info-grid{
      margin-top:12px;
      display:grid;
      grid-template-columns: repeat(2, minmax(0,1fr));
      gap:10px 18px;
    }
    .info-item{
      display:flex;
      flex-direction:column;
      gap:6px;
    }
    .info-item .label{
      font-size:12px;
      color:var(--muted);
      font-weight:600;
    }
    .info-item .value{
      font-size:15px;
      font-weight:600;
      color:#0f172a;
    }

    /* Education & Career: single column on small screens */
    .career-list{
      display:flex;
      flex-direction:column;
      gap:8px;
      margin-top:10px;
    }
    .career-row{
      display:flex;
      justify-content:space-between;
      color:var(--muted);
      font-size:14px;
      gap:8px;
      align-items:center;
    }
    .career-row .key{ font-weight:600; color:var(--muted); }
    .career-row .val{ font-weight:700; color:#0f172a; }

    /* Family list */
    .family-list{
      margin-top:8px;
      display:flex;
      flex-direction:column;
      gap:8px;
    }
    .family-item{
      display:flex;
      gap:12px;
      align-items:flex-start;
    }
    .family-item .role{
      width:110px;
      font-size:13px;
      color:var(--muted);
      font-weight:600;
    }
    .family-item .desc{
      font-weight:700;
      color:#0f172a;
      font-size:14px;
    }

    /* Quote */
    .quote{
      text-align:center;
      padding:18px;
      font-style:italic;
      color:#0f172a;
      background:linear-gradient(180deg, rgba(255,255,255,0.6), rgba(255,255,255,0.5));
      border-radius:12px;
      box-shadow: var(--shadow);
    }

    /* Small helpers */
    .muted { color:var(--muted); font-size:13px; }
    .caps { text-transform:uppercase; letter-spacing:0.09em; font-size:12px; color:var(--muted); }

    /* Responsive */
    @media (min-width:720px){
      .cards{
        grid-template-columns: 1fr 1fr;
      }
      .profile-card{
        padding:40px 26px 26px 26px;
      }
      .avatar-wrap{
        margin-top:-84px;
      }
      /* Make the header span columns */
      .profile-card { grid-column: 1 / -1; }
      .card--large { grid-column: 1 / -1; }
    }

    @media (max-width:420px){
      .avatar { width:110px; height:110px; }
      .upload-btn { right:-4px; bottom:-4px; padding:5px; font-size:12px; }
      .info-grid{ grid-template-columns:1fr; }
    }

  </style>
</head>
<body>
  <div class="wrap">
    <div class="container" role="main" aria-labelledby="profileName">

      <!-- PROFILE HEADER -->
      <section class="profile-card" aria-labelledby="profileHeader">
        <div class="avatar-wrap" style="display:flex;justify-content:center;">
          <div class="avatar" id="avatarInitials" aria-hidden="true">AF</div>
          <img id="profileImage" alt="Profile photo of Abdul Fahath S" style="display:none;border-radius:999px;position:relative;z-index:1;" />
          <!-- upload label -->
          <label class="upload-btn" title="Upload or change photo">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
              <path d="M12 16V6" stroke="#2563eb" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M8 10l4-4 4 4" stroke="#2563eb" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M20 21H4a2 2 0 0 1-2-2V7" stroke="#2563eb" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <span style="font-weight:700;">Change</span>
            <input id="photoInput" type="file" accept="image/*" aria-label="Upload profile photo">
          </label>
        </div>

        <div style="margin-top:12px;">
          <div id="profileName" class="name">Abdul Fahath S</div>
          <div class="profession">Software Engineer (Developer) • NielsenIQ</div>
          <div class="muted" style="margin-top:6px;">Annual Package: 17 LPA</div>
        </div>
      </section>

      <!-- CARDS -->
      <div class="cards" aria-live="polite">

        <!-- Personal Info -->
        <article class="card card--large" aria-labelledby="personalTitle">
          <h3 id="personalTitle">Personal Information</h3>
          <p class="lead">Basic details and contact-ready summary</p>

          <div class="info-grid">
            <div class="info-item">
              <div class="label">Date of Birth</div>
              <div class="value">07 January 1998</div>
            </div>
            <div class="info-item">
              <div class="label">Age</div>
              <div class="value">27</div>
            </div>

            <div class="info-item">
              <div class="label">Religion</div>
              <div class="value">Islam</div>
            </div>
            <div class="info-item">
              <div class="label">Location</div>
              <div class="value">Chennai, Tamil Nadu</div>
            </div>
          </div>
        </article>

        <!-- Education & Career -->
        <article class="card" aria-labelledby="eduTitle">
          <h3 id="eduTitle">Education & Career</h3>
          <div class="career-list">
            <div class="career-row"><div class="key">Qualification</div><div class="val">B.E. Automobile Engineering</div></div>
            <div class="career-row"><div class="key">Profession</div><div class="val">Software Engineer (Developer)</div></div>
            <div class="career-row"><div class="key">Company</div><div class="val">NielsenIQ</div></div>
            <div class="career-row"><div class="key">Annual Package</div><div class="val">17 LPA</div></div>
          </div>
        </article>

        <!-- Family Background -->
        <article class="card" aria-labelledby="familyTitle">
          <h3 id="familyTitle">Family Background</h3>
          <div class="muted" style="margin-top:6px;font-size:13px;">Immediate family details</div>

          <div class="family-list">
            <div class="family-item">
              <div class="role">Father</div>
              <div class="desc">Shahul Hameed A — Own Business</div>
            </div>

            <div class="family-item">
              <div class="role">Mother</div>
              <div class="desc">Najemunisha S — Homemaker</div>
            </div>

            <div class="family-item">
              <div class="role">Sibling</div>
              <div class="desc">One Younger Sister — Shafana Banu A (Married)</div>
            </div>
          </div>
        </article>

      </div>

      <!-- Quote -->
      <div style="width:100%; margin-top:10px;">
        <div class="quote">
          "Seeking a life partner to walk together in faith, respect, and a future filled with happiness."
        </div>
      </div>

      <!-- Small footer note -->
      <p class="muted" style="margin-top:12px;font-size:13px;text-align:center;">Profile created by Abdul Fahath S — share this link with prospective families</p>

    </div>
  </div>

  <script>
    // Photo upload preview logic
    (function(){
      const input = document.getElementById('photoInput');
      const img = document.getElementById('profileImage');
      const initials = document.getElementById('avatarInitials');

      input.addEventListener('change', async (e) => {
        const file = e.target.files && e.target.files[0];
        if (!file) return;

        // Validate file type (basic)
        if (!file.type.startsWith('image/')) {
          alert('Please select a valid image file.');
          input.value = '';
          return;
        }

        const reader = new FileReader();
        reader.onload = function(ev){
          img.src = ev.target.result;
          img.style.display = 'block';
          // hide initials background when image exists
          if (initials) initials.style.display = 'none';
        };
        reader.readAsDataURL(file);
      });

      // Make the avatar clickable to open file picker (for accessibility)
      const avatarWrap = document.querySelector('.avatar-wrap');
      avatarWrap && avatarWrap.addEventListener('click', () => {
        input.click();
      });
    })();
  </script>
</body>
</html>